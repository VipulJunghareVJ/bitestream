name: bitestream
version: "1.0.0"
config-version: 2

profile: bitestream

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  bitestream:
    silver:
      +materialized: view
    gold:
      +materialized: table
